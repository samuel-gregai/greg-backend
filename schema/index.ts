export type LoginType = {
    email: string;
    password: string;
}
export type SignupType = {
    firstname: string;
    lastname: string;
    email: string;
    password: string;
    confirmPassword: string;
}

